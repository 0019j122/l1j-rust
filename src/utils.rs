// 這裡定義函數，不要加 pub 也可以，只要在同一個文件內
fn get_xy_delta(heading: i32) -> (i32, i32) {
    match heading {
        0 => (0, -1), 1 => (1, -1), 2 => (1, 0), 3 => (1, 1),
        4 => (0, 1), 5 => (-1, 1), 6 => (-1, 0), 7 => (-1, -1),
        _ => (0, 0),
    }
}